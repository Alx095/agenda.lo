/** @deprecated Use useTheme() instead */
import { getLightTheme } from './palettes';

const light = getLightTheme();

export const colors = light.colors;
export const statusTheme = light.statusTheme;
export const navigationTheme = light.navigationTheme;
